import { PagePlaceholder } from "@/components/layout/page-placeholder";

export default function NoticesPage() {
  return (
    <PagePlaceholder
      title="Notices"
      description="Publish school-wide, branch-specific, or department-specific announcements for employees."
      badge="Announcements"
    />
  );
}