import { PagePlaceholder } from "@/components/layout/page-placeholder";

export default function LeavesPage() {
  return (
    <PagePlaceholder
      title="Leaves"
      description="Submit, review, approve, reject, and track employee leave requests with leave balance support."
      badge="Leave Management"
    />
  );
}