import { PagePlaceholder } from "@/components/layout/page-placeholder";

export default function AttendancePage() {
  return (
    <PagePlaceholder
      title="Attendance"
      description="View time-in/time-out records, attendance details, photos, GPS coordinates, addresses, status badges, manual corrections, and pending reviews."
      badge="Attendance Records"
    />
  );
}