import { PagePlaceholder } from "@/components/layout/page-placeholder";

export default function RfidPage() {
  return (
    <PagePlaceholder
      title="RFID Cards"
      description="Assign RFID cards, focus scanner input automatically, prevent duplicate active RFID UID, replace active card, disable cards, and keep RFID history."
      badge="RFID Assignment"
    />
  );
}